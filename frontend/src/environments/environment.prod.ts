// Stage B: Replace the apiUrl below with your deployed backend URL before building for production
// e.g. apiUrl: 'https://your-backend.onrender.com'
export const environment = {
    production: true,
    apiUrl: 'https://YOUR_BACKEND_URL_HERE'
};
